version: 2.0
jobs:
  build:
    docker:
      - image: timothystewart6/trinitycore-docker
        command: [-it, --rm, -v "$PWD/artifacts":/artifacts, -r "https://github.com/timothystewart6/BotsPlusPlus.git", -b master]