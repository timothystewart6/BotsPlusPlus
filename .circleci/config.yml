version: 2
jobs:
  build:
    docker:
      - image: timothystewart6/trinitycore-docker   # (1)
    steps:
      - checkout
      # ... steps for building/testing app ...
      - setup_remote_docker   # (2)
      - run: docker run -it --rm -v "$PWD/artifacts":/artifacts timothystewart6/trinitycore-docker -r "https://github.com/timothystewart6/BotsPlusPlus.git" -b master
